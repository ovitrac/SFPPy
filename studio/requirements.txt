# =============================================================================
# SFPPy Studio — pip Requirements
# =============================================================================
#
# Dependencies for Studio (interactive web GUI).
# Assumes SFPPy core is already installed.
#
# Usage:
#   pip install -r studio/requirements.txt
#
# Or install from SFPPy root (recommended):
#   pip install -e ".[studio]"
#
# Alternative installation methods:
#   - Root environment.yml        (conda, full installation)
#   - studio/environment.yml      (conda, studio-specific)
#   - requirements/studio.txt     (modular pip requirements)
#   - docker/docker-compose.yml   (Docker deployment)
#
# Launch application:
#   python studio/launcher.py           # Default port 8002
#   python studio/launcher.py --port 9002  # Custom port
#
# =============================================================================

# ---------------------------------------------
# Core Scientific Stack (SFPPy kernel)
# ---------------------------------------------
numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.5.0
pandas>=1.3.0

# ---------------------------------------------
# Configuration & File I/O
# ---------------------------------------------
pyyaml>=6.0
openpyxl>=3.1.0              # XLSX export
pillow>=8.0.0                # Images (PubChem thumbnails)

# ---------------------------------------------
# Web Application
# ---------------------------------------------
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
jinja2>=3.1.0
python-multipart>=0.0.6
requests>=2.28.0             # PubChem API
