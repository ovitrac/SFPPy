# SFPPy Survey Module Requirements
# =================================
#
# Install with:
#   pip install -r requirements.txt
#
# Note: Assumes base SFPPy is already installed (numpy, scipy, pyyaml)

# Core dependencies (already in SFPPy)
numpy>=1.20.0
scipy>=1.7.0
pyyaml>=6.0
matplotlib>=3.5.0

# FastAPI application
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
jinja2>=3.1.0
pydantic>=2.0.0

# Spreadsheet support
openpyxl>=3.1.0  # XLSX files
odfpy>=1.4.0     # ODS files (optional)

# PubChem integration
requests>=2.28.0
